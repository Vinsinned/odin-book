extends layout

block content

  h1 Delete Comment: #{comment.name}

  p Do you really want to delete this comment?
  
  form(method='POST' action='')
    div.form-group
      input#id.form-control(type='hidden',name='id', required='true', value=comment._id )

    button.btn.btn-primary(type='submit') Delete