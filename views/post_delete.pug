extends layout

block content

  h1 Delete Post: #{post.name}

  p Do you really want to delete this Post?
  
  form(method='POST' action='')
    div.form-group
      input#id.form-control(type='hidden',name='id', required='true', value=post._id )

    button.btn.btn-primary(type='submit') Delete